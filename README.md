unified cloud-based platform that integrates secure file storage with online code execution for multiple programming languages (Python, C++, Java, and JavaScript).
The frontend (React.js) provides a responsive interface, while the Flask backend manages APIs, authentication, and execution logic.
Supabase handles database and storage services.
The final deployment on AWS Elastic Beanstalk ensures scalability, reliability, and performance for end users.
